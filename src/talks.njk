---
layout: main
---

{% set css %}{% include "talks.css" %}{% endset %}
<style type="text/css">{{ css | cssmin | safe }}</style>

<h1>Talks</h1>

<p>An archive of past and future speaking engagements. I’ve been speaking to audiences large and small since the late 1990s on a variety of topics from software to technology. If you’d like me to speak at your company, event, conference, please email: <a href="mailto:me@duncandavidson.com">me@duncandavidson.com</a>.</p>

<ol>
  {% for event in data.events %}
    <li class="event">
      <div class="meta">{{ event.when }} &bull; {{ event.where }}</div>
      <div class="name">
        {% if event.link %}
          <a href="{{event.link}}">{{ event.name }}</a>
        {% else %}
          {{ event.name }}
        {% endif %}
      </div>
      
      <div>
        <ul>
          {% for talk in event.talks %}
             <li class="talk">
                {% if talk.link %}
                  <a href="{{talk.link}}">{{ talk.title }}</a>
                {% else %}
                  {{ talk.title }}
                {% endif %}
             </li>
          {% endfor %}
        </ul>
      </div>
    </li>
  {% endfor %}
</ol>